---
---

<section class="relative min-h-screen flex items-center overflow-hidden pt-16">
  <!-- Background gradient -->
  <div class="absolute inset-0 bg-gradient-to-br from-primary-50 via-stone-50 to-sage-50"></div>

  <!-- Decorative blobs -->
  <div class="absolute top-20 right-0 w-96 h-96 bg-primary-200/30 rounded-full blur-3xl -translate-y-1/2 translate-x-1/4"></div>
  <div class="absolute bottom-0 left-0 w-80 h-80 bg-sage-200/20 rounded-full blur-3xl translate-y-1/3 -translate-x-1/4"></div>
  <div class="absolute top-1/2 left-1/2 w-64 h-64 bg-sky-200/20 rounded-full blur-3xl -translate-x-1/2 -translate-y-1/2"></div>

  <div class="relative max-w-6xl mx-auto px-6 py-24 lg:py-32 grid lg:grid-cols-2 gap-16 items-center">
    <!-- Text content -->
    <div class="animate-fade-up">
      <!-- Badge -->
      <div class="inline-flex items-center gap-2 bg-primary-100 text-primary-700 px-4 py-1.5 rounded-full text-sm font-medium mb-6">
        <span class="w-2 h-2 bg-primary-500 rounded-full"></span>
        ADHD-Friendly by Design
      </div>

      <h1 class="text-5xl lg:text-6xl font-bold text-stone-900 leading-tight mb-6">
        Clean home,<br />
        <span class="text-primary-600">calm mind.</span>
      </h1>

      <p class="text-xl text-stone-600 leading-relaxed mb-10 max-w-lg">
        Finally, a cleaning app that gets how your brain works. No overwhelming lists, no guilt trips — just gentle guidance to help you maintain a tidy home without the stress.
      </p>

      <!-- Download buttons -->
      <div id="download" class="flex flex-col sm:flex-row gap-4">
        <!-- Google Play -->
        <a
          href="https://play.google.com/store/apps/details?id=com.forgekit.tidy"
          target="_blank"
          rel="noopener noreferrer"
          class="inline-flex items-center gap-3 bg-stone-900 hover:bg-stone-800 text-white px-6 py-3.5 rounded-xl transition-all hover:scale-105 shadow-lg"
        >
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="currentColor">
            <path d="M3.18 23.76c.3.17.64.24.99.2l12.6-11.53-3.24-3.24L3.18 23.76zm16.7-12.9L16.6 8.6l-3.36 3.08 3.36 3.36 3.3-1.9a1.6 1.6 0 000-2.28zM3 .73a1.6 1.6 0 00-.82 1.42v19.7c0 .6.3 1.1.82 1.41l.13.08L14.3 12.07v-.3L3.13.65 3 .73zm9.95 11.62L3.05 1.47c.3-.04.62.03.9.2l12.6 7.28-3.6 3.4z"/>
          </svg>
          <div class="text-left">
            <div class="text-xs opacity-75">Get it on</div>
            <div class="font-semibold text-sm">Google Play</div>
          </div>
        </a>

        <!-- App Store (coming soon) -->
        <div class="inline-flex items-center gap-3 bg-stone-200 text-stone-500 px-6 py-3.5 rounded-xl cursor-not-allowed relative">
          <svg class="w-6 h-6" viewBox="0 0 24 24" fill="currentColor">
            <path d="M18.71 19.5c-.83 1.24-1.71 2.45-3.05 2.47-1.34.03-1.77-.79-3.29-.79-1.53 0-2 .77-3.27.82-1.31.05-2.3-1.32-3.14-2.53C4.25 17 2.94 12.45 4.7 9.39c.87-1.52 2.43-2.48 4.12-2.51 1.28-.02 2.5.87 3.29.87.78 0 2.26-1.07 3.8-.91.65.03 2.47.26 3.64 1.98l-.09.06c-.22.15-2.18 1.27-2.16 3.8.03 3.02 2.65 4.03 2.68 4.04-.03.07-.42 1.44-1.38 2.83M13 3.5c.73-.83 1.94-1.46 2.94-1.5.13 1.17-.34 2.35-1.04 3.19-.69.85-1.83 1.51-2.95 1.42-.15-1.15.41-2.35 1.05-3.11z"/>
          </svg>
          <div class="text-left">
            <div class="text-xs">Coming soon</div>
            <div class="font-semibold text-sm">App Store</div>
          </div>
          <div class="absolute -top-2 -right-2 bg-gold-400 text-stone-900 text-xs font-bold px-2 py-0.5 rounded-full">Soon</div>
        </div>
      </div>

      <!-- Social proof -->
      <div class="mt-10 flex items-center gap-4 text-sm text-stone-500">
        <div class="flex -space-x-2">
          <div class="w-8 h-8 rounded-full bg-primary-300 border-2 border-white flex items-center justify-center text-xs font-bold text-primary-800">A</div>
          <div class="w-8 h-8 rounded-full bg-sage-300 border-2 border-white flex items-center justify-center text-xs font-bold text-sage-800">J</div>
          <div class="w-8 h-8 rounded-full bg-sky-300 border-2 border-white flex items-center justify-center text-xs font-bold text-sky-800">M</div>
        </div>
        <span>Loved by the ADHD community</span>
      </div>
    </div>

    <!-- Phone mockup -->
    <div class="relative flex justify-center lg:justify-end animate-float">
      <div class="relative w-64 h-[520px]">
        <!-- Phone frame -->
        <div class="absolute inset-0 bg-stone-900 rounded-[2.5rem] shadow-2xl border-4 border-stone-800"></div>
        <!-- Screen -->
        <div class="absolute inset-2 rounded-[2rem] overflow-hidden bg-stone-50">
          <!-- App UI mockup -->
          <div class="h-full flex flex-col">
            <!-- Status bar -->
            <div class="h-10 bg-stone-50 flex items-center justify-between px-4 pt-2">
              <span class="text-xs font-medium text-stone-600">9:41</span>
              <div class="flex gap-1">
                <div class="w-3 h-1.5 bg-stone-400 rounded-sm"></div>
                <div class="w-1 h-1.5 bg-stone-300 rounded-sm"></div>
              </div>
            </div>
            <!-- App content -->
            <div class="flex-1 bg-stone-50 px-3 py-2 overflow-hidden">
              <div class="text-xs font-semibold text-stone-500 mb-1">Good morning ✨</div>
              <div class="text-sm font-bold text-stone-900 mb-3">Today's Tasks</div>

              <!-- Task items -->
              {["Vacuum living room", "Wipe kitchen counter", "Take out trash", "Fold laundry"].map((task, i) => (
                <div class={`flex items-center gap-2 mb-2 p-2 rounded-xl ${i === 0 ? "bg-primary-50 border border-primary-200" : "bg-white border border-stone-100"}`}>
                  <div class={`w-5 h-5 rounded-full border-2 flex items-center justify-center flex-shrink-0 ${i === 0 ? "bg-primary-500 border-primary-500" : "border-stone-300"}`}>
                    {i === 0 && <svg class="w-3 h-3 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="3" d="M5 13l4 4L19 7"/></svg>}
                  </div>
                  <span class={`text-xs ${i === 0 ? "line-through text-stone-400" : "text-stone-700"}`}>{task}</span>
                </div>
              ))}

              <!-- Streak card -->
              <div class="mt-3 bg-gradient-to-r from-gold-400 to-primary-500 rounded-xl p-3">
                <div class="text-white text-xs font-semibold mb-1">🔥 7-day streak!</div>
                <div class="text-white/80 text-xs">Keep it up — you're on a roll</div>
              </div>
            </div>
          </div>
        </div>
        <!-- Notch -->
        <div class="absolute top-2 left-1/2 -translate-x-1/2 w-20 h-5 bg-stone-900 rounded-full z-10"></div>
      </div>

      <!-- Floating badges -->
      <div class="absolute top-12 -left-4 bg-white rounded-2xl shadow-xl px-4 py-2.5 flex items-center gap-2 animate-fade-in">
        <span class="text-xl">✅</span>
        <div>
          <div class="text-xs font-bold text-stone-900">Task complete!</div>
          <div class="text-xs text-primary-600">+10 points</div>
        </div>
      </div>

      <div class="absolute bottom-20 -right-4 bg-white rounded-2xl shadow-xl px-4 py-2.5 flex items-center gap-2">
        <span class="text-xl">🔥</span>
        <div>
          <div class="text-xs font-bold text-stone-900">7-day streak</div>
          <div class="text-xs text-stone-500">Keep going!</div>
        </div>
      </div>
    </div>
  </div>
</section>
